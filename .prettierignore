# Handlebars partials with template syntax inside HTML attributes
src/partials/footer.html

blog/posts/

# Cloudflare Pages config (TOML is not supported by Prettier without a plugin)
wrangler.toml

# Cloudflare Pages headers file (extensionless; no Prettier parser)
public/_headers
